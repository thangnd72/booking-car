import { SIZE } from '@/helpers/size';
import theme from '@/helpers/theme';
import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  textInput: {
    paddingVertical: 8,
    borderBottomWidth: 1,
    fontSize: 14,
    borderBottomColor: theme.colors.lightTwoColor,
  },
});

export default styles;
